##
##  Makefile -- Build procedure for sample libconnect_mod Apache module
##  Autogenerated via ``apxs -n libconnect_mod -g''.
##

builddir=.
top_srcdir=/usr/local/apache2
top_builddir=/usr/local/apache2
include /usr/local/apache2/build/special.mk

CC :=   gcc

#   the used tools
APACHECTL=service apache

#   additional defines, includes and libraries
#DEFS=-Dmy_define=mod_pcap.h
INCLUDES=-I/usr/local/apache2/include -I/usr/local/apr/include/apr-1 -I/usr/local/apr-util/include/apr-1 -I.
LIBS=-L/ -lmodpcap

pcap: mod_pcap.c
	$(CC) $(INCLUDES) mod_pcap.c -fPIC -shared -o libmodpcap.so 

#   the default target
all: pcap local-shared-build 


#   install the shared object file into Apache 
install: install-modules-yes restart

#   cleanup
clean:
	-rm -f mod_libconnect_mod.o mod_libconnect_mod.lo mod_libconnect_mod.slo mod_libconnect_mod.la 

#   simple test
test: reload
	lynx -mime_header http://127.0.0.1/libconnect_mod

#   install and activate shared object by reloading Apache to
#   force a reload of the shared object file
reload: install restart

#   the general Apache start/restart/stop
#   procedures
start:
	$(APACHECTL) -k start
restart:
	$(APACHECTL) -k restart
stop:
	$(APACHECTL) -k stop

